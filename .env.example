# Used by root docker-compose.yml when building/running the full stack.
POSTGRES_PASSWORD=proauth_dev_password
JWT_SECRET=change_me
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
GROQ_ENABLED=true

# Browser-facing backend URL, baked into the frontend's static build.
VITE_API_BASE_URL=http://localhost:5001/api
